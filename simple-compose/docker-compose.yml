version: '3.6'
services:
    counter:
        build: .
        image: reg.istry.cz/dte/simple-compose/counter
        ports:
            - ${PORT:-80}:80
        depends_on:
            - redis
    redis:
        image: redis:alpine