version: '3.6'
services:
    counter:
        build: .
        image: reg.istry.cz/dte/multiple-composes/counter
        depends_on:
            - redis
        labels:
            traefik.enable: true
            traefik.frontend.rule: Host:localhost
    redis:
        image: redis:alpine
